'use strict'

let chai = require('chai')

describe('Pipeline', function() {
	it('mist initialize with minimum of history, stage and stop in its configuration')
	it('must be able to load configuration')
})